services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: server-monitor
    restart: unless-stopped
    ports:
      # Mapeo explícito al puerto 8090 (configurable mediante .env)
      - "${PORT:-8090}:8090"
    environment:
      - PORT=8090
      - TZ=UTC
    volumes:
      # Socket de Docker en modo solo lectura para escanear contenedores y métricas
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # Montaje para lectura precisa del disco del host
      - /:/host:ro
      # Montaje para detectar versión de Ubuntu Server y Hostname
      - /etc/os-release:/etc/host-os-release:ro
      - /etc/hostname:/etc/host_hostname:ro
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
        reservations:
          cpus: "0.05"
          memory: 48M
